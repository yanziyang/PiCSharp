namespace Pi.Cli;

/// <summary>Entry point for the <c>pi</c> command-line agent.</summary>
internal static class Program
{
    private static int Main(string[] args)
    {
        // Scaffold only. Real entry point is ported in T6.6 from
        // reference/pi/packages/coding-agent/src/cli.
        Console.Error.WriteLine("pi (PiCSharp): not implemented yet - scaffold only.");
        return 1;
    }
}
