# Skipped-test budget

Current budget: **9**

| Test | Reason | Clears when |
|---|---|---|
| `Pi.Tui.Tests.EditorHistoryKeybindingsTests` | `editor-history-keybindings.test.ts` exercises `Pi.Tui.Editor`, which is T5.3b and does not exist yet. It was listed in T5.3a's source table in error — the filename reads like a keybindings test but the subject is the editor. | T5.3b lands `components/editor.ts` |
| `Pi.Tui.Tests.TuiRenderImageTests` (7 cases) | The seven `tui-render.test.ts` cases that assert Kitty image row reservation, image-id deletion and placement geometry. T5.2b ports `tui.ts` against a stubbed image seam that reports no image protocol, so the stub cannot produce these outputs. Held as skips so the debt is counted rather than missing. | T5.5 lands `terminal-image.ts` |
| `TuiRenderTests.Skips_full_re_render_on_height_changes_in_termux` | `isTermuxSession()` is defined in `tui-main-screen.ts`, which T5.2b does not port, so the behaviour exists only in the `TestTui` double. It is also the only case that sets `TERMUX_VERSION` to a non-null value, which raced with the render thread reading it. | `tui-main-screen.ts` is ported |

Raising this budget requires a row here and a justification in the PR. Lowering it is always welcome.
